import React from 'react';
import styles from './styles.module.scss';

const SmallScreen = () => {
  return <div className={styles.smallScreen}>Viewport Too Small</div>;
};

export default SmallScreen;
